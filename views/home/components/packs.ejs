<div class="container">
  <div class="">
    <h1 class="p-3 border-bottom fw-bold">Packs</h1>
  </div>
  <div class="pricing-header p-3 pb-md-4 mx-auto text-center">
    <h1 class="display-4 fw-normal">Pricing</h1>
    <p class="fs-5 text-muted">
      Quickly build an effective pricing table for your potential customers with
      this Bootstrap example. It’s built with default Bootstrap components and
      utilities with little customization.
    </p>
  </div>
  <div class="mt-5 row row-cols-1 row-cols-md-3 mb-3 text-center">
    <% for (let pack of packs) {%>
    <div class="col">
      <div class="card mb-4 rounded-3 shadow-sm">
        <div class="card-header py-3">
          <h4 class="my-0 fw-normal"><%= pack.title %></h4>
        </div>
        <div class="">
          <h1 class="text-center text-success"><%= pack.profit %>%</h1>
          <ul class="list-unstyled mt-3 mb-4">
            <p class="m-3 text-start"><%= pack.description %></p>
          </ul>
          <button type="button" class="w-100 btn btn-lg btn-outline-primary">
            More details
          </button>
        </div>
      </div>
    </div>
    <% } %>
  </div>
</div>
